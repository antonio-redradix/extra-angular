export interface Complaint {
    name: string;
    code: string|number;
    number: string|number;
    date: string|number;
    priority: string|number;
    type: string|number;
    declaration: string;
    id: number;
}